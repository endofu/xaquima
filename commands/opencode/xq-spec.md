---
description: Search .agent/specs/ architecture docs (pass search term)
---
Search the project's architecture docs in `.agent/specs/` for: **$ARGUMENTS**

Available specs: !`ls .agent/specs/ 2>/dev/null || echo "No specs"`

1. Search all files in `.agent/specs/` for "$ARGUMENTS".
2. Show matching files with summaries and relevant sections.
3. Show cross-references. If no matches, list all spec files.
